const chai = require("chai");

const MovieModel = require("../build/models/Movie");
const GenereModel = require("../build/models/Genere");

describe("Movie Model", () => {
  describe("/", () => {
    it("should work", (done) => {
      done();
    });
  });
});
